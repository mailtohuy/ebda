const 
  chai = require('chai'),
  should = chai.should(),
  mocha = require('mocha'),
  auth = require('../auth');

describe('Auth',function(){
 it('getUser() should not mutate user list');
 it('authorize() should match paths by root');

});
